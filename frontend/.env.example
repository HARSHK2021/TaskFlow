# Frontend — copy to .env locally, add in Vercel Project Settings → Environment Variables

# Local dev: full backend URL
REACT_APP_API_URL=http://localhost:8000

# Production (Vercel Services): same-origin API path
# REACT_APP_API_URL=/api

REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id
